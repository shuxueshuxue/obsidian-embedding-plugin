{
  "name": "embedding",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=browser --target=es2018 --format=cjs --external:obsidian --outfile=main.js"
  },
  "devDependencies": {
    "esbuild": "0.19.2",
    "typescript": "5.4.5"
  }
}
